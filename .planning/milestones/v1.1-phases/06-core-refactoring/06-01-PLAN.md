---
phase: 06-core-refactoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/aisync-core/src/types.rs
  - crates/aisync-core/src/adapter.rs
  - crates/aisync-core/src/config.rs
  - crates/aisync-core/src/sync.rs
  - crates/aisync-core/src/init.rs
  - crates/aisync-core/src/diff.rs
  - crates/aisync-core/src/watch.rs
  - crates/aisync-core/src/conditional.rs
  - crates/aisync-core/src/detection.rs
  - crates/aisync/src/commands/init.rs
  - crates/aisync/src/commands/status.rs
  - crates/aisync/src/commands/sync.rs
  - crates/aisync/src/commands/diff.rs
  - crates/aisync/src/commands/check.rs
  - crates/aisync/src/commands/hooks.rs
  - crates/aisync/src/commands/watch.rs
autonomous: true
requirements:
  - REFAC-01

must_haves:
  truths:
    - "ToolKind::Custom(String) variant exists and can represent arbitrary tool names"
    - "ToolKind serializes as lowercase hyphenated strings (claude-code, cursor, opencode)"
    - "ToolKind no longer derives Copy — all usage sites use Clone"
    - "All existing tests pass after Copy-to-Clone migration"
  artifacts:
    - path: "crates/aisync-core/src/types.rs"
      provides: "ToolKind with Custom(String) variant, custom Serialize/Deserialize, as_str(), Display impl"
      contains: "Custom(String)"
  key_links:
    - from: "crates/aisync-core/src/types.rs"
      to: "all files referencing ToolKind"
      via: "Clone instead of Copy"
      pattern: "\\.clone\\(\\)"
---

<objective>
Extend ToolKind with Custom(String) variant and migrate from Copy to Clone across the entire codebase.

Purpose: ToolKind must support arbitrary tool names for Phase 10+ declarative TOML adapters. Adding Custom(String) makes ToolKind non-Copy, requiring a codebase-wide migration from implicit Copy to explicit Clone. This is the foundational change that all other Phase 6 refactoring builds on.

Output: ToolKind with Custom(String), custom serde, and all ~40 usage sites updated from Copy to Clone.
</objective>

<execution_context>
@/Users/pmannion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-core-refactoring/06-CONTEXT.md
@.planning/phases/06-core-refactoring/06-RESEARCH.md
@crates/aisync-core/src/types.rs
@crates/aisync-core/src/adapter.rs

<interfaces>
<!-- Current ToolKind — the primary refactoring target -->
From crates/aisync-core/src/types.rs:
```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub enum ToolKind {
    ClaudeCode,
    Cursor,
    OpenCode,
}
```

ToolKind is used in these structs (all derive Serialize and contain ToolKind fields):
- DetectionResult { tool: ToolKind }
- HookTranslation::Supported { tool: ToolKind }
- HookTranslation::Unsupported { tool: ToolKind }
- ToolDiff { tool: ToolKind }
- SyncAction::WriteHookTranslation { tool: ToolKind }
- SyncAction::WarnUnsupportedHooks { tool: ToolKind }
- ToolSyncResult { tool: ToolKind }
- ToolSyncStatus { tool: ToolKind }
- ToolMemoryStatus { tool: ToolKind }
- ToolHookStatus { tool: ToolKind }
- WatchEvent::ReverseSync { tool: ToolKind }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor ToolKind — add Custom(String), custom serde, remove Copy</name>
  <files>crates/aisync-core/src/types.rs</files>
  <behavior>
    - ToolKind::Custom("windsurf".to_string()) creates a valid variant
    - Serializing ToolKind::ClaudeCode produces "claude-code" (not "ClaudeCode")
    - Serializing ToolKind::Cursor produces "cursor"
    - Serializing ToolKind::OpenCode produces "opencode"
    - Serializing ToolKind::Custom("windsurf".to_string()) produces "windsurf"
    - Deserializing "claude-code" produces ToolKind::ClaudeCode (not Custom("claude-code"))
    - Deserializing "cursor" produces ToolKind::Cursor
    - Deserializing "opencode" produces ToolKind::OpenCode
    - Deserializing "windsurf" produces ToolKind::Custom("windsurf".to_string())
    - ToolKind::ClaudeCode.as_str() returns "claude-code"
    - Display impl uses as_str() (format!("{}", ToolKind::ClaudeCode) == "claude-code")
    - ToolKind no longer derives Copy
    - Existing test_tool_kind_clone_copy updated to remove Copy assertion
  </behavior>
  <action>
In `crates/aisync-core/src/types.rs`:

1. Remove `Copy` from ToolKind derive list. Remove `Serialize, Deserialize` from derive (will be custom). Keep `Debug, Clone, PartialEq, Eq, Hash`.

2. Add `Custom(String)` variant to ToolKind enum.

3. Add `as_str()` method to ToolKind:
```rust
impl ToolKind {
    pub fn as_str(&self) -> &str {
        match self {
            ToolKind::ClaudeCode => "claude-code",
            ToolKind::Cursor => "cursor",
            ToolKind::OpenCode => "opencode",
            ToolKind::Custom(s) => s.as_str(),
        }
    }
}
```

4. Implement `fmt::Display` for ToolKind using `as_str()`.

5. Implement custom `Serialize` for ToolKind — serialize as `self.as_str()`.

6. Implement custom `Deserialize` for ToolKind — match known strings to named variants, unknown strings to `Custom(s)`. This normalizes well-known names (deserializing "claude-code" always returns `ToolKind::ClaudeCode`, never `Custom("claude-code")`).

7. Update existing tests:
   - `test_tool_kind_clone_copy`: Remove Copy assertion (`let t2 = t;` line), keep Clone test.
   - `test_tool_kind_debug`: Update expected debug output — `Custom("windsurf")` should show as `Custom("windsurf")`.
   - Add new tests for serialization round-trip, Custom variant, as_str(), and Display.

Note: After this task, `cargo check -p aisync-core` will fail due to Copy removal in downstream files. That is expected — Task 2 fixes all usage sites.
  </action>
  <verify>
    <automated>cargo test -p aisync-core -- types::tests</automated>
  </verify>
  <done>ToolKind has Custom(String) variant, custom serde producing lowercase strings, as_str() method, Display impl, and no Copy derive. All types.rs tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate all ToolKind usage sites from Copy to Clone</name>
  <files>
    crates/aisync-core/src/adapter.rs,
    crates/aisync-core/src/config.rs,
    crates/aisync-core/src/sync.rs,
    crates/aisync-core/src/init.rs,
    crates/aisync-core/src/diff.rs,
    crates/aisync-core/src/watch.rs,
    crates/aisync-core/src/conditional.rs,
    crates/aisync-core/src/detection.rs,
    crates/aisync/src/commands/init.rs,
    crates/aisync/src/commands/status.rs,
    crates/aisync/src/commands/sync.rs,
    crates/aisync/src/commands/diff.rs,
    crates/aisync/src/commands/check.rs,
    crates/aisync/src/commands/hooks.rs,
    crates/aisync/src/commands/watch.rs
  </files>
  <action>
Run `cargo check --workspace 2>&1` iteratively to find all compile errors from Copy removal. For each error:

**Pattern 1 — Implicit copy in struct field access:** `result.tool` used after move → add `.clone()`.
Example: `tool_display_name(tool_status.tool)` → `tool_display_name(tool_status.tool.clone())`

**Pattern 2 — Function taking ToolKind by value:** Change parameter to `&ToolKind` if the function doesn't need ownership, or keep by-value and ensure caller clones.
For the `tool_display_name(tool: ToolKind)` functions in CLI commands: change to `tool_display_name(tool: &ToolKind)` since they only match on it. Update all call sites to pass `&tool_status.tool` instead of `tool_status.tool`.

**Pattern 3 — ToolKind in match arms used after match:** Add `.clone()` before the match or borrow in the match.

**Pattern 4 — Structs containing ToolKind that derive Copy:** If any struct derives Copy and contains ToolKind, remove Copy from that struct's derives too. (Check DetectionResult — it only derives Debug, Clone, so should be fine.)

**Pattern 5 — ToolKind in SyncAction/HookTranslation Display:** The Display impl for SyncAction uses `{:?}` format for tool fields. These are borrowed in the match, so should work. Verify.

**Approach:** Run `cargo check --workspace` after each file change. Fix errors one file at a time. The compiler will catch every instance — there are no silent failures.

**Key files with known ToolKind references (from research):**
- `sync.rs`: `enabled_tools()` constructs ToolKind values — these are literals, no Copy needed
- `init.rs`: `ToolKind` in detection results — needs `.clone()` on field access
- `watch.rs`: `ToolKind` in WatchEvent construction — needs `.clone()`
- `conditional.rs`: `ToolKind` in match — likely just needs borrow
- CLI commands: `tool_display_name()` functions — change to take `&ToolKind`
  </action>
  <verify>
    <automated>cargo test --workspace</automated>
  </verify>
  <done>Full workspace compiles. All tests pass. No remaining Copy usage of ToolKind anywhere in the codebase. `cargo check --workspace` produces zero errors.</done>
</task>

</tasks>

<verification>
1. `cargo test --workspace` — all tests pass
2. `cargo clippy --workspace` — no warnings related to ToolKind
3. Verify ToolKind serialization: `cargo test -p aisync-core -- types::tests` shows custom serde working
4. Verify no remaining `Copy` derive on ToolKind: grep for `Copy` in types.rs ToolKind derive line returns nothing
</verification>

<success_criteria>
- ToolKind has Custom(String) variant for arbitrary tool names
- ToolKind serializes as clean lowercase strings ("claude-code", not "ClaudeCode")
- Copy derive removed; entire workspace compiles with Clone
- All existing tests pass, new serde tests pass
- Foundation ready for Plan 02 (trait expansion) and Plan 03 (BTreeMap config)
</success_criteria>

<output>
After completion, create `.planning/phases/06-core-refactoring/06-01-SUMMARY.md`
</output>
