---
phase: 05-polish-and-distribution
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/release.yml
autonomous: false
requirements: [DIST-02, DIST-03, DIST-04, DIST-05]
user_setup:
  - service: github
    why: "Release workflow needs write permissions on the repository"
    env_vars: []
    dashboard_config:
      - task: "Ensure repo is public (or configure GitHub token with release permissions)"
        location: "GitHub repo Settings -> General -> Danger Zone"
      - task: "Enable workflow permissions: Settings -> Actions -> General -> Workflow permissions -> Read and write"
        location: "GitHub repo Settings -> Actions -> General"

must_haves:
  truths:
    - "CI runs cargo test on macOS, Linux, and Windows on every push/PR"
    - "Tagging a version triggers release workflow that builds binaries for all platforms"
    - "GitHub Release contains downloadable binaries for macOS (arm64, x86_64), Linux (x86_64), Windows (x86_64)"
    - "Release includes a shell installer script"
    - "Homebrew formula is generated for the release"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Cross-platform test CI"
      contains: "matrix"
    - path: ".github/workflows/release.yml"
      provides: "Release workflow with binary builds"
      contains: "release"
  key_links:
    - from: ".github/workflows/release.yml"
      to: "Cargo.toml version"
      via: "Tag push triggers build"
      pattern: "tags"
    - from: ".github/workflows/ci.yml"
      to: "cargo test"
      via: "Matrix strategy across OS"
      pattern: "cargo test"
---

<objective>
Set up cross-platform CI testing and release automation using cargo-dist for GitHub Releases, Homebrew formula, and shell installer.

Purpose: DIST-02 (Homebrew), DIST-03 (GitHub releases), DIST-04 (shell installer), DIST-05 (cross-platform CI).
Output: CI workflow running tests on macOS/Linux/Windows, release workflow generating binaries and distribution channels.
</objective>

<execution_context>
@/Users/pmannion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish-and-distribution/05-RESEARCH.md
@.planning/phases/05-polish-and-distribution/05-01-SUMMARY.md
@Cargo.toml
@crates/aisync/Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create cross-platform CI workflow</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Create .github/workflows/ci.yml with:

1. Trigger on push and pull_request to main branch.

2. A "test" job with matrix strategy:
   - os: [ubuntu-latest, macos-latest, windows-latest]
   - Steps:
     - actions/checkout@v4
     - dtolnay/rust-toolchain@stable
     - Swatinem/rust-cache@v2
     - `cargo test --workspace` (runs all unit + integration tests)
     - `cargo clippy --workspace -- -D warnings` (lint check)

3. A "format" job (single OS, ubuntu-latest):
   - Steps:
     - actions/checkout@v4
     - dtolnay/rust-toolchain@stable with components: rustfmt
     - `cargo fmt --all -- --check`

Note on Windows: symlink tests may fail. If existing unit tests have symlink-dependent tests, they should already be gated with #[cfg(unix)]. If CI reveals failures, a follow-up gap closure plan will address them. Do NOT preemptively gate tests -- let CI surface real issues.
  </action>
  <verify>
    <automated>cd /Users/pmannion/whiskeyhouse/agentsync && cat .github/workflows/ci.yml | head -40</automated>
  </verify>
  <done>ci.yml exists with matrix strategy for macOS/Linux/Windows, runs cargo test and cargo clippy.</done>
</task>

<task type="auto">
  <name>Task 2: Configure cargo-dist and generate release workflow</name>
  <files>.github/workflows/release.yml, Cargo.toml</files>
  <action>
1. Install cargo-dist if not present: `cargo install cargo-dist`

2. Run `cargo dist init` to configure the project. When prompted (or via flags):
   - Select targets: x86_64-apple-darwin, aarch64-apple-darwin, x86_64-unknown-linux-gnu, x86_64-pc-windows-msvc
   - Enable: GitHub Releases (CI), Homebrew installer, shell installer
   - This will:
     - Add [dist] or [dist.workspace] metadata to Cargo.toml (or create dist-workspace.toml)
     - Generate .github/workflows/release.yml

3. If cargo-dist generates a release.yml, review it and ensure:
   - Triggers on tag push matching `v*` pattern
   - Builds for all 4 targets
   - Creates GitHub Release with binary assets
   - Includes Homebrew formula generation
   - Includes shell installer generation

4. If cargo-dist adds metadata to Cargo.toml, keep it. The tool manages this configuration.

5. If cargo-dist interactive mode is problematic in non-TTY, use flags:
   `cargo dist init --yes --with-json-config` or manually create the config based on cargo-dist documentation.
   Fallback: If cargo-dist is unavailable or too complex to run non-interactively, create the release.yml manually based on the research document's CI matrix pattern and standard cargo-dist output format. The key structure is:
   - Tag-triggered workflow
   - Matrix build across 4 targets using cross-compilation
   - Upload artifacts to GitHub Release
   - Generate checksums

Important: cargo-dist evolves quickly. If the exact flags differ from documentation, adapt. The goal is a working release pipeline, not exact flag matching.
  </action>
  <verify>
    <automated>cd /Users/pmannion/whiskeyhouse/agentsync && test -f .github/workflows/release.yml && echo "release.yml exists" && head -20 .github/workflows/release.yml</automated>
  </verify>
  <done>release.yml exists and is configured to build cross-platform binaries on tag push. Cargo.toml has dist metadata.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify CI and release configuration</name>
  <files>.github/workflows/ci.yml, .github/workflows/release.yml</files>
  <action>Human reviews the CI and release workflow files generated by Tasks 1 and 2.</action>
  <verify>
    <automated>cd /Users/pmannion/whiskeyhouse/agentsync && test -f .github/workflows/ci.yml && test -f .github/workflows/release.yml && echo "Both workflow files exist"</automated>
  </verify>
  <done>User has approved the CI and release configuration.</done>
  <what-built>Cross-platform CI workflow (.github/workflows/ci.yml) and release pipeline (.github/workflows/release.yml) with cargo-dist configuration for GitHub Releases, Homebrew formula, and shell installer.</what-built>
  <how-to-verify>
    1. Review .github/workflows/ci.yml -- should have matrix with ubuntu, macos, windows
    2. Review .github/workflows/release.yml -- should trigger on tag push, build 4 targets
    3. Push branch to GitHub and verify CI workflow triggers
    4. Optionally: create a test tag to verify release workflow (can defer to actual release)
    5. Check Cargo.toml for cargo-dist metadata (if added)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues with the CI/release configuration</resume-signal>
</task>

</tasks>

<verification>
- .github/workflows/ci.yml exists with cross-platform matrix
- .github/workflows/release.yml exists with tag-triggered release
- Both YAML files are valid (no syntax errors)
- cargo build still succeeds after any Cargo.toml changes
</verification>

<success_criteria>
CI workflow tests on 3 platforms. Release workflow builds binaries for 4 targets, generates GitHub Release with Homebrew formula and shell installer. User has verified the configuration.
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-and-distribution/05-03-SUMMARY.md`
</output>
