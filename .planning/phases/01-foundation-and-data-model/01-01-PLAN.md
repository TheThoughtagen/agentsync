---
phase: 01-foundation-and-data-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/aisync-core/Cargo.toml
  - crates/aisync-core/src/lib.rs
  - crates/aisync-core/src/types.rs
  - crates/aisync-core/src/error.rs
  - crates/aisync-core/src/config.rs
  - crates/aisync/Cargo.toml
  - crates/aisync/src/main.rs
  - fixtures/claude-only/CLAUDE.md
  - fixtures/claude-only/.claude/.gitkeep
  - fixtures/cursor-only/.cursor/rules/.gitkeep
  - fixtures/cursor-legacy/.cursorrules
  - fixtures/opencode-only/AGENTS.md
  - fixtures/opencode-only/opencode.json
  - fixtures/multi-tool/CLAUDE.md
  - fixtures/multi-tool/.cursor/rules/.gitkeep
  - fixtures/multi-tool/opencode.json
  - fixtures/multi-tool/AGENTS.md
  - fixtures/ambiguous/AGENTS.md
  - fixtures/no-tools/.gitkeep
autonomous: true
requirements:
  - CLI-08

must_haves:
  truths:
    - "Cargo workspace compiles with `cargo build` from workspace root"
    - "aisync.toml can be parsed from a TOML string into AisyncConfig struct"
    - "AisyncConfig can be serialized back to valid TOML"
    - "Schema version validation rejects versions other than 1"
    - "Per-tool config with defaults inheritance works (tool overrides global defaults)"
  artifacts:
    - path: "Cargo.toml"
      provides: "Workspace root with members and shared dependencies"
      contains: "[workspace]"
    - path: "crates/aisync-core/src/config.rs"
      provides: "Config types and parsing"
      exports: ["AisyncConfig", "ToolsConfig", "ToolConfig", "DefaultsConfig", "SyncStrategy"]
    - path: "crates/aisync-core/src/types.rs"
      provides: "Shared types"
      exports: ["ToolKind", "Confidence"]
    - path: "crates/aisync-core/src/error.rs"
      provides: "Error hierarchy"
      exports: ["AisyncError", "ConfigError", "DetectionError", "AdapterError"]
    - path: "crates/aisync-core/src/lib.rs"
      provides: "Public API re-exports"
  key_links:
    - from: "crates/aisync-core/src/config.rs"
      to: "crates/aisync-core/src/types.rs"
      via: "SyncStrategy import"
      pattern: "use crate::types::SyncStrategy"
    - from: "crates/aisync-core/src/lib.rs"
      to: "all modules"
      via: "pub mod + pub use re-exports"
      pattern: "pub mod config"
    - from: "crates/aisync/src/main.rs"
      to: "crates/aisync-core"
      via: "dependency import"
      pattern: "use aisync_core"
---

<objective>
Scaffold the Cargo workspace, define shared types and error hierarchy, and implement config parsing for aisync.toml.

Purpose: Establishes the compilable Rust workspace that all future phases build on, and delivers the config schema (CLI-08) as a fully parsable/serializable data model.
Output: Compilable workspace with two crates, config types, error types, shared types, and test fixtures.
</objective>

<execution_context>
@/Users/pmannion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-data-model/01-CONTEXT.md
@.planning/phases/01-foundation-and-data-model/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold Cargo workspace, shared types, and error hierarchy</name>
  <files>
    Cargo.toml,
    crates/aisync-core/Cargo.toml,
    crates/aisync-core/src/lib.rs,
    crates/aisync-core/src/types.rs,
    crates/aisync-core/src/error.rs,
    crates/aisync/Cargo.toml,
    crates/aisync/src/main.rs
  </files>
  <behavior>
    - ToolKind enum has variants: ClaudeCode, Cursor, OpenCode
    - Confidence enum has variants: High, Medium
    - AisyncError converts from ConfigError via #[from]
    - AisyncError converts from DetectionError via #[from]
    - ConfigError::UnsupportedVersion holds version and expected fields
    - DetectionError::ScanFailed holds path and source fields
    - All error types implement Display and std::error::Error
    - `cargo build` succeeds from workspace root
  </behavior>
  <action>
    Create the workspace root Cargo.toml with:
    - `[workspace]` with `members = ["crates/*"]`
    - `[workspace.dependencies]` for serde (1.0, features=["derive"]), toml (1.0), thiserror (2.0)
    - `[workspace.package]` with edition = "2024" and rust-version = "1.85"

    Create crates/aisync-core/Cargo.toml using workspace inheritance for edition, rust-version, and all three dependencies.

    Create crates/aisync-core/src/types.rs with:
    - `ToolKind` enum: ClaudeCode, Cursor, OpenCode (derive Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)
    - `Confidence` enum: High, Medium (derive Debug, Clone, Copy, PartialEq, Eq)

    Create crates/aisync-core/src/error.rs with thiserror-derived error hierarchy:
    - `AisyncError` top-level: Config(#[from] ConfigError), Detection(#[from] DetectionError), Adapter { tool: String, source: AdapterError }
    - `ConfigError`: ReadFile(#[from] std::io::Error), Parse(#[from] toml::de::Error), UnsupportedVersion { version: u32, expected: u32 }
    - `DetectionError`: ScanFailed { path: String, source: std::io::Error }
    - `AdapterError`: DetectionFailed(String)

    Create crates/aisync-core/src/lib.rs with pub mod declarations for types, error, config (config added in Task 2). Re-export key types via `pub use`.

    Create crates/aisync/Cargo.toml with aisync-core path dependency. Create crates/aisync/src/main.rs as a minimal placeholder: `fn main() { println!("aisync v0.1.0"); }`.
  </action>
  <verify>
    <automated>cd /Users/pmannion/whiskeyhouse/agentsync && cargo build 2>&1 && cargo test 2>&1</automated>
  </verify>
  <done>Workspace compiles, both crates build, types.rs exports ToolKind and Confidence, error.rs exports full error hierarchy with thiserror derives, binary crate links to core.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement config schema parsing and test fixtures</name>
  <files>
    crates/aisync-core/src/config.rs,
    crates/aisync-core/src/lib.rs,
    fixtures/claude-only/CLAUDE.md,
    fixtures/claude-only/.claude/.gitkeep,
    fixtures/cursor-only/.cursor/rules/.gitkeep,
    fixtures/cursor-legacy/.cursorrules,
    fixtures/opencode-only/AGENTS.md,
    fixtures/opencode-only/opencode.json,
    fixtures/multi-tool/CLAUDE.md,
    fixtures/multi-tool/.cursor/rules/.gitkeep,
    fixtures/multi-tool/opencode.json,
    fixtures/multi-tool/AGENTS.md,
    fixtures/ambiguous/AGENTS.md,
    fixtures/no-tools/.gitkeep
  </files>
  <behavior>
    - Parsing a valid aisync.toml with schema_version=1 succeeds
    - Parsing with schema_version=2 returns ConfigError::UnsupportedVersion
    - Missing [defaults] and [tools] sections parse to defaults (empty/default values)
    - Per-tool config with sync_strategy overrides global default
    - SyncStrategy serializes as lowercase: "symlink", "copy", "generate"
    - TOML key "claude-code" maps to Rust field claude_code via serde rename
    - Round-trip: parse then serialize produces equivalent TOML
    - Config with all fields populated parses correctly
  </behavior>
  <action>
    Create crates/aisync-core/src/config.rs with:
    - `AisyncConfig` struct: schema_version (u32), defaults (DefaultsConfig with serde(default)), tools (ToolsConfig with serde(default))
    - `DefaultsConfig` struct: sync_strategy (SyncStrategy with serde(default))
    - `ToolsConfig` struct: claude_code (Option<ToolConfig> with serde(rename="claude-code")), cursor (Option<ToolConfig>), opencode (Option<ToolConfig>)
    - `ToolConfig` struct: enabled (bool, default true), sync_strategy (Option<SyncStrategy>)
    - `SyncStrategy` enum: Symlink (default), Copy, Generate — with serde(rename_all="lowercase")
    - `AisyncConfig::from_str(s: &str) -> Result<Self, ConfigError>` — parses TOML string, validates schema_version == 1
    - `AisyncConfig::from_file(path: &Path) -> Result<Self, ConfigError>` — reads file then calls from_str
    - `AisyncConfig::to_string_pretty(&self) -> Result<String, toml::ser::Error>` — serializes to TOML
    - `ToolConfig::effective_sync_strategy(&self, defaults: &DefaultsConfig) -> SyncStrategy` — returns tool override or global default

    Update lib.rs to add `pub mod config;` and re-export config types.

    Create test fixtures directory structure:
    - fixtures/claude-only/ — CLAUDE.md (minimal content) + .claude/.gitkeep
    - fixtures/cursor-only/ — .cursor/rules/.gitkeep
    - fixtures/cursor-legacy/ — .cursorrules (minimal content)
    - fixtures/opencode-only/ — AGENTS.md + opencode.json (minimal valid JSON: {})
    - fixtures/multi-tool/ — CLAUDE.md + .cursor/rules/.gitkeep + opencode.json + AGENTS.md
    - fixtures/ambiguous/ — AGENTS.md only (no opencode.json)
    - fixtures/no-tools/ — .gitkeep only

    Write tests in config.rs (#[cfg(test)] module):
    - test_parse_minimal_config: only schema_version=1, everything else defaults
    - test_parse_full_config: all fields populated
    - test_reject_wrong_schema_version: schema_version=2 fails
    - test_sync_strategy_override: tool-level strategy overrides default
    - test_round_trip: parse then serialize then parse again matches
    - test_claude_code_rename: "claude-code" TOML key works
  </action>
  <verify>
    <automated>cd /Users/pmannion/whiskeyhouse/agentsync && cargo test -p aisync-core -- config 2>&1</automated>
  </verify>
  <done>Config parsing works for all aisync.toml shapes: minimal, full, per-tool overrides. Schema version validation rejects non-1. Round-trip serialization works. All 7 fixture directories exist with correct marker files.</done>
</task>

</tasks>

<verification>
- `cargo build` compiles both crates without errors or warnings
- `cargo test -p aisync-core` passes all config tests
- `ls fixtures/` shows all 7 fixture directories
- `cargo run -p aisync` prints version placeholder
</verification>

<success_criteria>
- Workspace with aisync-core (lib) and aisync (bin) compiles on Rust 2024 edition
- AisyncConfig parses valid aisync.toml, rejects invalid schema versions
- SyncStrategy, ToolKind, Confidence enums defined and exported
- Error hierarchy with thiserror derives compiles and converts correctly
- Test fixtures for all 7 tool detection scenarios exist on disk
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-data-model/01-01-SUMMARY.md`
</output>
