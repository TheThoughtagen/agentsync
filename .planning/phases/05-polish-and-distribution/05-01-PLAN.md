---
phase: 05-polish-and-distribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/aisync/Cargo.toml
  - crates/aisync-core/Cargo.toml
  - crates/aisync/src/main.rs
autonomous: true
requirements: [CLI-10, DIST-01]

must_haves:
  truths:
    - "aisync completions bash outputs valid bash completion script"
    - "aisync completions zsh outputs valid zsh completion script"
    - "aisync completions fish outputs valid fish completion script"
    - "cargo publish --dry-run succeeds for both crates (metadata valid)"
  artifacts:
    - path: "crates/aisync/src/main.rs"
      provides: "Completions subcommand using clap_complete"
      contains: "Completions"
    - path: "crates/aisync/Cargo.toml"
      provides: "Publishing metadata and clap_complete dependency"
      contains: "clap_complete"
    - path: "crates/aisync-core/Cargo.toml"
      provides: "Publishing metadata or publish = false"
      contains: "publish"
  key_links:
    - from: "crates/aisync/src/main.rs"
      to: "clap_complete"
      via: "generate() call in Completions handler"
      pattern: "clap_complete::generate"
---

<objective>
Add shell completion generation (bash/zsh/fish) via clap_complete and prepare Cargo.toml metadata for crates.io publishing.

Purpose: CLI-10 (shell completions) and DIST-01 (cargo install readiness) are foundational for all distribution work.
Output: Working `aisync completions <shell>` subcommand, Cargo.toml files with required crates.io metadata.
</objective>

<execution_context>
@/Users/pmannion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish-and-distribution/05-RESEARCH.md
@crates/aisync/src/main.rs
@crates/aisync/Cargo.toml
@crates/aisync-core/Cargo.toml
@Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add clap_complete dependency and Completions subcommand</name>
  <files>Cargo.toml, crates/aisync/Cargo.toml, crates/aisync/src/main.rs</files>
  <action>
1. Add clap_complete to workspace dependencies in root Cargo.toml:
   `clap_complete = "4.5"`

2. Add clap_complete dependency in crates/aisync/Cargo.toml:
   `clap_complete = { workspace = true }`

3. In crates/aisync/src/main.rs, add a hidden Completions variant to the Commands enum:
   ```
   /// Generate shell completions
   #[command(hide = true)]
   Completions {
       /// Shell to generate completions for (bash, zsh, fish)
       shell: clap_complete::Shell,
   },
   ```

4. Add the handler in the main match block:
   ```
   Commands::Completions { shell } => {
       let mut cmd = Cli::command();
       clap_complete::generate(shell, &mut cmd, "aisync", &mut std::io::stdout());
       Ok(())
   }
   ```

5. Add `use clap::CommandFactory;` import at top of main.rs (needed for Cli::command()).
  </action>
  <verify>
    <automated>cd /Users/pmannion/whiskeyhouse/agentsync && cargo build --quiet 2>&1 && cargo run -- completions bash | head -5 && cargo run -- completions zsh | head -5 && cargo run -- completions fish | head -5</automated>
  </verify>
  <done>aisync completions bash/zsh/fish each produce non-empty shell completion scripts to stdout. The completions subcommand is hidden from --help output.</done>
</task>

<task type="auto">
  <name>Task 2: Add crates.io publishing metadata to Cargo.toml files</name>
  <files>crates/aisync/Cargo.toml, crates/aisync-core/Cargo.toml</files>
  <action>
1. Update crates/aisync/Cargo.toml with required publishing metadata:
   - description = "Sync AI tool configurations across Claude Code, Cursor, and OpenCode"
   - license = "MIT"
   - repository = "https://github.com/pmannion/agentsync"
   - readme = "../../README.md"
   - keywords = ["ai", "sync", "cli", "claude", "cursor"]
   - categories = ["command-line-utilities", "development-tools"]

2. Update crates/aisync-core/Cargo.toml:
   - Set `publish = false` under [package] (it's an internal library, not a standalone crate)
   - Add description = "Core library for aisync" for completeness

3. Verify the workspace root Cargo.toml does not need additional metadata (workspace itself is not published).

Note: Use the actual GitHub repository URL. If the repo is at a different URL, the executor should use whatever is correct based on the git remote.
  </action>
  <verify>
    <automated>cd /Users/pmannion/whiskeyhouse/agentsync && cargo package --package aisync --allow-dirty --no-verify 2>&1 | tail -5</automated>
  </verify>
  <done>cargo package for aisync succeeds (metadata valid). aisync-core has publish = false set.</done>
</task>

</tasks>

<verification>
- `cargo build` compiles without warnings
- `cargo run -- completions bash` outputs a completion script containing "aisync"
- `cargo run -- completions zsh` outputs a completion script
- `cargo run -- completions fish` outputs a completion script
- `cargo run -- --help` does NOT show "completions" subcommand (hidden)
- `cargo package --package aisync --allow-dirty --no-verify` succeeds
</verification>

<success_criteria>
Shell completions work for all three shells. Cargo.toml metadata passes crates.io validation. aisync-core is marked publish = false.
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-and-distribution/05-01-SUMMARY.md`
</output>
